

const Buscador = ({onChange}) => {
  return (
    <div className="pb-3 ">
        <input type="tex" placeholder="ingresa un nombre" onChange={onChange} />
        
    </div>
  )
}

export default Buscador